<!DOCTYPE html>
<html lang="en">
<head>
  <title>PDF Summarizer</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .fade-in {
      animation: fadeIn 0.8s ease-in-out both;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="min-h-screen bg-gray-900 flex items-center justify-center text-white">
  <div class="bg-gray-800 p-8 rounded-xl shadow-lg w-full max-w-md text-center fade-in">
    <h1 class="text-3xl font-bold text-white mb-4">📄 PDF Summarizer</h1>
    <p class="text-gray-300 mb-6 text-sm">Upload a research paper PDF to get a summary and voice version.</p>

    <form action="/upload" method="POST" enctype="multipart/form-data" class="space-y-4">
      <input 
        type="file" 
        name="pdf" 
        accept=".pdf" 
        required 
        class="w-full text-sm text-gray-200 p-2 border border-gray-600 rounded bg-gray-700 file:bg-blue-600 file:text-white file:rounded file:px-3 file:py-1 transition hover:scale-[1.01]"
      />
      <button 
        type="submit" 
        class="w-full bg-blue-600 hover:bg-blue-700 text-white py-2 rounded transition-transform transform hover:scale-105 duration-300"
      >
        Upload & Generate
      </button>
    </form>
  </div>
</body>
</html>
